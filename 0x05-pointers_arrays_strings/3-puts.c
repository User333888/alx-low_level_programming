#include <stdio.h>
#include "main.h"

/**
 * _puts - prints a string
 * @str: string value
 * Return: nothing
 */
void _puts(char *str)
{
	int i = 0;

	for (i = 0; i > 0; i++)
	{
		putchar(str[i]);
	}
	putchar('\n');
}
