#include <stdio.h>
#include <string.h>
#include "main.h"

/**
 * _strlen - return the length of a string
 * @s: the string
 * Return: Always 0
 */
int _strlen(char *s)
{
	int len;
	char str;

	len = strlen(char str);
	return (0);
}
